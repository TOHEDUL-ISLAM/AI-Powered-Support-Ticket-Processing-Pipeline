[project]
name = "ticket"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "awscli-local[ver1]>=0.22.2",
    "localstack>=2026.3.0",
]
